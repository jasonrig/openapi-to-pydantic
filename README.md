# OpenAPI to Pydantic Generator

This repository is initialized for a `uv`-managed Python project that will implement the behavior defined in `SPEC.md`.

## Status

Project scaffolding only. Generator implementation has not started.

## Tooling

- Dependency and environment management: `uv`
- Tests: `pytest`
- Lint/format: `ruff`, `pylint`
- Typing: `mypy`
